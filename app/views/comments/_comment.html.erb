<p>
    <%= comment.body %>
      <% if comment.persisted? %>
      <%= link_to "削除",
            post_comment_path(@post, comment),
            data: { turbo_method: :delete, turbo_confirm: "削除する？" } %>
    <% end %>
  </p>